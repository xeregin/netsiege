for i in `nova list | grep ACTIVE | sed 's/.*=//' | sed 's/ .*//' | sed 's/,//'`; do ip netns exec $(ip netns | grep `neutron net-list | grep private | awk '{print $2}'`) ping -c 1 -W 1 $i | grep packet; done
